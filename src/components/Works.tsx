import React from "react";

const Works: React.FC = () => {
  return (
    <section id="works" className="portfolio-card">
      <h2>Works</h2>
      <p>これまで作ったプロジェクトや作品を紹介する。</p>
      なし
    </section>
  );
};

export default Works;
